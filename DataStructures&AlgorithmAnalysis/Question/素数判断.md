# 素数判断

```cpp
#include<iostream>
using namespace std;
int judge_shushu(int a)
{
	if(a==1)
		return 0;
	else if(a==2)
		return 1;
	else
	{
		int b=2;
		for(;b<a;)
		{
			if(!(a%b))
			{
				return 0;
				break;
			}
			else b++;

		}
		if(b==a)
			return 1;
	}
	
}
```